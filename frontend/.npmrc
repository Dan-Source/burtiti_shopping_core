# .npmrc (na raiz do projeto)
# Permitir scripts de build
ignore-scripts=false

# Especificar que sharp é seguro
only-built-dependencies=sharp,unrs-resolver

# Ou aprovar todos automaticamente
enable-pre-post-scripts=true